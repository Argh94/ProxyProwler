name: ProxyProwler

on:
  workflow_dispatch:
  schedule:
    - cron: '0 23 * * *'  # اجرا در 23:00 UTC (2:30 صبح IRST)

jobs:
  scrape-and-release:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        
